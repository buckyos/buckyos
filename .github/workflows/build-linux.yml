name: Build Buckyos Linux Components
on: 
    workflow_call:
        inputs:
            arch:
                required: true
                type: string
            enable_tests:
                required: false
                type: boolean
                default: false

jobs:
  build:
    name: build
    runs-on: ${{ inputs.arch == 'aarch64' && 'ubuntu-24.04-arm' || 'ubuntu-24.04' }}
    steps:
    - name: Checkout
      uses: actions/checkout@v6
    - name: Setup pnpm
      uses: pnpm/action-setup@v4
      with:
        version: "latest"
    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '24.x'
    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        toolchain: stable
        targets: ${{inputs.arch}}-unknown-linux-${{inputs.arch == 'aarch64' && 'gnu' || 'musl'}}
    - name: Setup Needed Packages
      if: ${{ inputs.arch == 'x86_64' }}
      run: |
          set -euxo pipefail
          sudo apt update
          sudo apt install -y musl-tools g++ binutils

          tool_dir="$RUNNER_TEMP/musl-toolchain-bin"
          mkdir -p "$tool_dir"

          native_gxx="$(command -v x86_64-linux-gnu-g++ || command -v g++)"
          native_ar="$(command -v x86_64-linux-gnu-ar || command -v ar)"
          native_ranlib="$(command -v x86_64-linux-gnu-ranlib || command -v ranlib)"

          cat > "$tool_dir/x86_64-linux-musl-g++" <<EOF
          #!/bin/sh
          exec "$native_gxx" "\$@" -specs /usr/lib/x86_64-linux-musl/musl-gcc.specs
          EOF
          chmod +x "$tool_dir/x86_64-linux-musl-g++"

          ln -sf "$native_ar" "$tool_dir/x86_64-linux-musl-ar"
          ln -sf "$native_ranlib" "$tool_dir/x86_64-linux-musl-ranlib"
          ln -sf "$tool_dir/x86_64-linux-musl-g++" "$tool_dir/musl-g++"
          ln -sf "$tool_dir/x86_64-linux-musl-ar" "$tool_dir/musl-ar"
          ln -sf "$tool_dir/x86_64-linux-musl-ranlib" "$tool_dir/musl-ranlib"
          echo "$tool_dir" >> "$GITHUB_PATH"
          export PATH="$tool_dir:$PATH"

          command -v x86_64-linux-musl-gcc
          command -v x86_64-linux-musl-g++
          command -v x86_64-linux-musl-ar
          command -v x86_64-linux-musl-ranlib
    - name: cargo test
      working-directory: ./src
      if: ${{inputs.enable_tests == 'true'}}
      run: cargo test -- --test-threads=1
    - name: install buckyos devkit
      run: pip install "buckyos-devkit @ git+https://github.com/buckyos/buckyos-devkit.git"
    - name: build buckyos
      working-directory: ./src
      run: buckyos-build
    - name: Get Version
      id: get_version
      run: |
        full_version=`./src/rootfs/bin/node-daemon/node_daemon --version|grep -oP '\d+(?:\.\d+)*\+build\S+'`
        echo "full_version=$full_version" >> $GITHUB_OUTPUT
        short_version=`./src/rootfs/bin/node-daemon/node_daemon --version|grep -oP '\d+(?:\.\d+)*\+build\d+'`
        echo "short_version=$short_version" >> $GITHUB_OUTPUT
    - name: Tar Rootfs
      run:
        cd ./src/rootfs && tar -cvf ../../rootfs.tar .
    - name: Prepare tools
      run: |
        mkdir -p tools
        cp ./src/rootfs/bin/buckycli/buckycli ./tools/
    - name: Upload tools
      uses: actions/upload-artifact@v6
      with:
        name: buckyos-tools-linux-${{inputs.arch == 'x86_64' && 'amd64' || 'aarch64'}}-${{steps.get_version.outputs.short_version}}
        path: tools
        if-no-files-found: error
    - name: Upload
      uses: actions/upload-artifact@v6
      id: upload
      with:
        name: buckyos-linux-${{inputs.arch == 'x86_64' && 'amd64' || 'aarch64'}}-${{steps.get_version.outputs.short_version}}
        path: rootfs.tar
        if-no-files-found: error
    - name: Report Upload Urls
      run: |
        npm install @buckyos/test-server-client
        npx test-server-client seturl buckyos ${{steps.get_version.outputs.short_version}} linux ${{inputs.arch}} ${{steps.upload.outputs.artifact-url}} ${{github.sha}}
      env:
        USERNAME: ${{ secrets.TEST_SERVER_USERNAME }}
        PRIVATE_KEY: ${{ secrets.TEST_SERVER_PRIVATE_KEY }}
        ENDPOINT: "https://buckyos.ai"

