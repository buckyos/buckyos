{
  "name": "nfs-server-test",
  "private": true,
  "type": "module",
  "scripts": {
    "smoke": "deno run --allow-net --allow-read --allow-write --allow-env --unsafely-ignore-certificate-errors nfs_server_dv.ts",
    "test": "pnpm run smoke"
  },
  "dependencies": {
    "buckyos": "latest"
  }
}
