# BuckyOS 包管理系统：NodeDaemon 通过 PackageEnv 实现自动安装与升级流程

## 一、包加载（Load Package）的几种方式

### 1. 按包名加载（开发模式）

最常用的方式。使用者调用 `load_package` 时只传入 **package name**。

- NodeDaemon 通过 `load_package("app_name")` 获取该包的实际目录
- PackageEnv 负责返回实际目录路径
- NodeDaemon 拿到目录后，调用其中的启动脚本来启动 APP
- 这与开发模式下的目录结构天然吻合

### 2. 按包名 + 版本表达式加载

`load_package` 时传入 package name 及**版本运算符**，支持三种情况：

| 情况 | 说明 |
|------|------|
| 不带版本表达式 | 加载带有 `latest` 标签的版本；若无 `latest` 标签，则默认取版本号最大的 |
| 范围表达式 | 如 `>1.0, <2.0`，PackageEnv 支持类似 OS 包管理器的复杂版本运算 |
| 精确版本号 | 锁定到某个确定版本 |

> **备注：** PackageEnv 的设计早于 BuckyOS，最初是为了解决 Python 多依赖问题而设计的基础组件，因此天然支持复杂的版本运算。但对 BuckyOS 而言，**版本运算符能力基本用不上**——原因是我们的依赖关系 100% 不是文件级的，而是通过服务端点实现的。在分布式系统中，A 依赖 B 实际上是通过调度器获取服务 B 的端点后进行调用，各组件甚至不在同一个进程中。

### 3. 按包名 + ObjectID（哈希）精确加载

这是 BuckyOS 中**真正区分开发形态与运行形态**的关键：

- **开发形态：** 所有包未经发布，没有哈希，退回到按包名加载
- **运行形态：** 通过应用商店、系统更新等正规渠道获取的版本，都带有 ObjectID（哈希），使用包名 + 哈希加载；在有哈希的情况下，甚至连包名都不需要

调度器下发的配置文件中隐含了这个区分逻辑：
- 不带哈希 → 按包名加载（开发版）
- 带哈希 → 精确加载（正式版）

### 4. 混合模式

在开发版系统中也可以走正常的安装流程（从应用商店下载）。此时会出现混合情况：

- 部分组件通过包名加载（本地编译构建的）
- 部分组件通过哈希加载（从应用商店安装的）

> **遗留问题：** PackageEnv 有严格模式和非严格模式。严格模式依赖内部数据库做包名与哈希的匹配校验；非严格模式（开发模式）走目录搜索。混合使用两种模式时——即一个包在数据库中不存在但目录中存在——需要确认 PackageEnv 是否支持这种配置。过去的设计假设开发模式和正式模式不会混用，但现在实际上是混用的。

---

## 二、自动升级流程

### 1. 进程存活判断（判活）

NodeDaemon 本质上是**状态推演**逻辑：不断根据调度器下发的配置（目标状态）来检测目标进程是否存在。

**核心问题：** 传统的检测方式是基于包名/进程名，无法判断当前运行的进程是否是调度器配置文件中指定的那个带哈希的精确版本。

**改进方向：**

- 如果调度器下发了哈希，判断进程是否存在时必须判断其**完整路径**（PackageEnv 的实现保证目录路径中一定包含哈希）
- 或者由 NodeDaemon 维护一个列表，每次启动后写入 PID + 包名 + 包哈希的映射

> **权衡：** 维护列表的方式更准确，但排除了状态推导能力——它假设所有进程都是自己拉起的。如果是其他方式（如运维人员手动启动）启动了目标版本的服务，列表方式无法感知。而状态推演方式下，NodeDaemon 只要看到目标 ObjectID 对应的进程在运行，就不做任何操作。

### 2. 判活的效果

基于 ObjectID 判活有一个好处：**不区分新版本还是旧版本**，只要不是目标版本在运行，就认为"不存在"，从而触发启动新版本。

### 3. Lazy Install（懒安装）

启动新版本前，先判断 PackageEnv 中是否已安装该包：

1. 如果未安装，通过 **PackageID**（命名对象）从分布式对象存储（NamedStore）中获取
2. NamedStore 是系统中**性能最高的分布式存储组件**，比文件系统更轻量
3. 通过 ObjectID 打开 Reader → Unzip 到 PackageEnv 的实际目录

**关键特性：**

- **原子性安装：** 安装过程 100% 原子性，安装前已对包的哈希做过校验，永远不存在"下载到一半"或"安装到一半"的问题
- **自修复能力：** 即使目录被误删除，只要分布式对象存储还在，就能自动还原
- 这也是为什么一定要用 ObjectID 的原因——分布式系统如果依赖传统运维脚本，必然会碰到"装一半"的问题

### 4. 启动与停止脚本的设计

- 所有启动脚本的**第一行**是执行停止操作
- **停止脚本**使用通配符杀进程（泛化停止，停止该包所有版本）
- **检测脚本**使用精确路径检测

> **注意事项：** 这种"有的地方用精确控制，有的地方用泛化控制"的细节逻辑非常关键，任何一处搞错都会导致状态不一致。当前这些脚本由 NodeDaemon 依靠 PackageEnv 的路径规则拼接而成，不够显式，多次出现工程师误改破坏状态依赖的情况。**建议将这套脚本作为 PackageEnv 基础设施的一部分来升级维护。**

---

## 三、卸载与版本清理

### 清理旧版本的策略

对于非开发版（严格版本模式），采用**定期全量清理 + Lazy Install 恢复**的策略：

1. NodeDaemon 设置一个每日清理点
2. 在清理点将 PackageEnv 中所有解压目录全部删除
3. 通过 Lazy Install 机制，需要的包会被自动恢复

这种方式避免了设计复杂的旧版本清理逻辑。

### NodeDaemon 自身的升级

NodeDaemon 作为维护服务，自身也需要升级：

1. 挂载一个极小的辅助脚本
2. 该脚本负责：删除旧版 NodeDaemon → 通过 Lazy Install 拉取新版 → 启动
3. NodeDaemon 不是直接启动的进程，而是通过一个**永远不变的 start 脚本**启动

> 这样可以避免 PackageEnv 长期运行后出现"版本爆炸"问题（类似 Python 依赖爆炸、npm 依赖爆炸），引入 ObjectID + Lazy Install 后，通过简单的全量清理即可解决。

---

## 四、PackageEnv Database 的角色变化

### 过去的逻辑

- PackageEnv 内部有一个 meta 数据库，支持版本运算、latest 标签等功能
- 自动升级依赖将本地数据库与服务器源（Source）的数据库做同步
- 同步后 latest 版本改变 → 触发 Lazy Install → 拉取新版本

### 现在的变化

在当前 BuckyOS 架构下：

- 不再需要重型的本地数据库同步机制
- 每个节点只需要缓存调度器下发的那些包的 meta 信息即可
- 而这些 meta 恰好在调度器创建运行进程时就已经保存了
- 旧的数据库语义在当前环境下不应该再使用
- 甚至可以退化成纯开发模式语义，避免纠结数据库数据正确性的问题
- 清理缓存很容易，但过去从未清理过这个数据库（因为数据量小被忽略了）

> **结论：** 如果所有数据库内容都是从调度器下发的配置中零散收集的，那么整个 PackageEnv 目录基本可以回到清空状态。

---

## 五、关于 PackageEnv 组件的反思

### 现状

PackageEnv 是一个非常早期的核心组件，最初引入时理所当然（任何系统都需要包管理和自动升级），还做了 Python 到 Rust 的移植。

### 反思

现在来看，BuckyOS **不需要这么强力的组件**。实际需要的核心功能：

- 一个 `install` 函数（即 Lazy Install）
- 一组目录规则
- 没有数据库、没有版本运算、没有缓存之后，一切都变得比较纯粹

**纠结点：**

- 删除或简化一个运行很久的老组件是否会带来风险？
- 修改上游 PackageEnv 以适配 BuckyOS 新需求，是否会让 PackageEnv 对其他使用方不够纯粹？
- 可能一个由 NodeDaemon 管理的、更轻量级的简单目录规则组件就足够了

---

## 六、包更新的完整流程（发行者视角）

### 第一步：发行者发布新版本

1. 构建包，生成 ObjectID
2. 上传到自己的 **RepoServer**
3. 将 **PackageMeta**（包含最新版本信息）广播出去

### PackageMeta 的三个存储/查询位置

| 位置 | 说明 |
|------|------|
| **BNS（区块链命名服务）** | 最权威、最稳定，但更新成本较高，一般不作为首选 |
| **包索引器（Source）** | 允许使用者订阅多个源，PackageMeta 都带签名，下载时一定会做本地验证 |
| **发行者自身（OOD）** | 通过 owner 名字反推出 OOD 域名，使用标准 Repo 协议直接查询 ObjectID 或按包名列出所有发行版本 |

发行者至少需要将 PackageMeta 存入以上三个位置中的**至少一个**。

### 第二步：待升级端（OOD）获取更新

1. 通过某种方式（自动/手动）发现新版本的 PackageMeta
2. **Collect（收藏）：** 将 PackageMeta 元数据收藏到自己的 Repo
3. 通知用户发现新版本（或根据系统配置自动下载）
4. 将 PackageMeta 对应的文件下载到 Repo 的 NamedStore

> **本质：** 这是一个与包加载无关的、业务无关的通用逻辑——"同一个名字的对象，其 ObjectID 发生了更新"。

### 第三步：用户触发升级

1. 在调度器中将应用配置文件的 ObjectID 从旧版本改为新版本
2. 调度器下发新配置到 NodeDaemon
3. NodeDaemon 在目标节点上执行：
   - 判活：发现目标版本进程不存在
   - Lazy Install：从 NamedStore 中拉取并安装
   - 启动新版本进程（启动脚本第一行先停止旧版本）

### 核心容错设计

> **系统可靠性保证在调度器构造配置文件之前。** 对于底层基础设施来说，走到它们的流程时，都假设所需文件已经存在于 NamedStore 中。它们不关心文件从哪里来，只需要知道"你要我运行指定版本，这个版本所需的文件必须在 Store 里已经准备好"，然后通过 Lazy Install 完成即可。

## 附录 语音转文字原文

```text
我记录一下,用语音说一下,现在BackOS在这个Packageían就是这个包管理,实际的包管理,就是最后一步,这个NodeDaemon去依通过这个Packageían实现这个自动的安装和升级的这个基本流程的整理。

首先第一个就是我们为了开发方便,其实从这个使用者的角度来讲的话,用的最多的这个就是load package,因为load package传什么很重要。如果是开发阶段的话,我们通常喜欢传的是package name,就相当于说如果说我要是运行一个APP,对吧,那node daemon它就需要通过这个load pack, load pack这个APP的名字去得到它的实际的目录,那个package因为会把这个实际的目录告诉node daemon,然后node daemon它拿到这个目录之后呢,它就可以去调用里面的这个启动脚本去启动这个APP,这个是最简单一种情况啊,我们它跟我们在开发模式下的目录结构也是吻合的。

然后呢,从从这个入口出发的第二个就是loadpackage的时候呢,我传的不光是一个package name了,还有两种逻辑,第一种逻辑就是传一个所谓的版本那个版本的这个运算符进来,就相当于说它渴望就如果说这个在env里面说存在多版本啊,当然说package env这个东西它的这个设计比8kos要早,它最早的时候是是解决这个python下面的多依赖的问题。这个这个就所设计的一个非常非常非常早的基础组件哈,所以说它是支持像OS的这样的很复杂的这种版本运算名,比如说呃你希望下载的一个package name这个版本大于1.0,小于2.0类似这样的东西,我们是可以支持呃这个这个带版本运算符的去下载这个package的。但这个能力呃目前对8kos来讲的话,它其实不是那么的重要,因为根本原因是什么呢?根本原因是我们的依赖关系,就应用和应用和应用以及服务之间的关系,这个依赖关系百分之百都不是通过文件级的,都是通过这个服务,就相当于说其实他根本不在乎你的啊,这个这个因为大家都甚至不在一个进程上嘛,分布式系统里面你A依赖B其实都是依赖,都是通过调度器得到了这个B的这个服务B的端点之后你来过去依赖它,也就是说在对于我们这种分布式文件系统呃分布式系统来讲,这个package因为提供了这种呃基于版本运算符的这种依赖关系,这种能力呃在它的角度来讲,它有这个能力,但其实我们基本用不上。

然后第三个方法我们叫精确加载,精确加载有两种级别的精确,第一种就是精确指定这个包的某个版本,就是说换句话讲,在这个整个的推演下面,如果基于版本号逻辑就变成说原来的package name这个版本是加载的,是有lasty的标签的这个版本,如果没有lasty的标签的话,那么默认的这个latest就是所谓的版本号最大的那一个。 然后这个这个package因为它也支持说版本号这个如果说你不用版本表达式,用的是一个确认型版本的话,这个它就它就会会这个锁定一个版本,这个呃你可以认为是前面的两种,换句话讲,这是版本这个包名加上版本表达式的一个呃一个一个三种情况吧,就是不带表达式,带一个范围表达式和带一个精确表达式,这是这个叫做精确加载,但但我们呃BugOS本身就是说你填一个精确的符号这个事情,其实你系统都自动运行的嘛,就是说对我们来讲的话,呃你这个精确的版本号,这个开发期间肯定不会用的,因为开发期间期间你不可能版本一升级你就去用,你肯定都是依赖这个呃最新版本的这个,因为开发版你在系统里有些只有一个版本嘛,所以说你用这个默认机制刚好就符合这个逻辑,但我们真正运行起来的时候,我们其实我们竟然都我们既然这个版本号是通过上游传下来的,就一层层配置文件传下来的,但其实配置文件里面在BugOS里它更喜欢用的是name的objectid, 就是说它是一个哈希的东西,就你可以理解这个理解成哈希就好了,就是说我这个版本名后面带上一个哈希,意思就是说这个这个它因为站在分布系统的角度,它是一个精确匹配的东西,就它其实连版本号都不是那么在意,对吧,反正配置文件里面我安装的时候我已经做过报交验了,那我把哈希的扔过来之后,其实你在加载的时候,这个看得更清楚,对吧,也就是说这个有没有这个用哈希下载和这个不用哈希下载,其实才是我们呃BugOS里面我们真正区分开发开发式形态和运行形态的这个这个真正区别 就开发形态的话,因为你的所有的包其实都没有没有做发布,所以说你得不到哈希,对吧,那他就回退回那个用包名加载,但在正式版里面你通过这种正常的应用商店啊,对吧,系统分更新啊,对吧,你拿到的这个版本,它通常都是带有这个都是都是带有这个呃版本哈希的对吧,对,所以说你会呃你就有机会说通过这个包名加哈希下载,当然说再有哈希的情况下,其实连包名都不用了

站在这个立场上就可以去分析一下我们这个升级的过程,就这里面会有一个很有意思的一个话题啊,首先第一个,我在一个开发版的系统里面,对吧,我能不能安装正常走正常的安装流程,对吧,如果不能那肯定要能走啊,不能走的话,很多开发测试就走不了,也就是说,在开发版的情况下会出现说,有些组件是用包名加载的,对吧,但你在这里面只要从应用商店去下载一个新应用,那这个应用其实它就通过的是这个这个包包的这个哈希去加载了,对吧,说这是一种混合的情况啊,对吧,就说你不能说只能说是这个所有的这个构编译出来的这个构建出来的这个包是用包名加载的,然后呃当然说这个其实其实它就隐含的隐含的一个含义吧,就相当于说我们这个系统里面所有的这个呃因为我们是是所有的nodedaemon都是通过这个调度器下发的配置文件去决定在本地运行什么APP呃启动哪些进程的嘛。那它启动进程的时候,这个调度器下发的这个这个配置文件里面它就一个隐含的假设,对,如果是是是开发版的话,那么这个配置文件就不带这个哈希,对吧,那它就是用包名加载,就一旦带了哈希,那它就用包名加哈希加载,就是一个精确加载,对啊,就是说相当于说对于这对我们的package底层的这个package系统来讲的话,就是说第一那个package因位来讲的话,它也是默认就支持这种形式的,但是它有严格模式和非严格模式啊,就就严格模式的核心逻辑还是在于在于它得有个数据库,就是说它在那个package因位里面它有个数据库,它得在那个数据库里面把这个包名和哈希匹配上,它才它才可以加载成功,就就非严格模式开发模式下就没这个数据库,它就基本走目录搜索就完事了,对,但我不太记得这个,这可能要看一下代码,因为这是个比较比较比较早期的技术组件了,就一个包,如果说我在这里面我混合这两种模式,我加载一个包,这个包其实数据库里根本不存在,但目录里有,对,我得去看下面下面能不能配置成说支持这种,我觉得这个其实是就相当于说它允许就从原来那种开发者模式,就原来认为这个开发者模式和这个真正跑的模式不会混用嘛,但现在实际上是混用的。


在这个假设下,我们下面看那个自动升级啊,现在讲的是node包,那node包之前,就是这个加包之前,其实加包其实已经在启动的前一步了哈,但是我们在我们在这个整个的nodedemon它其实跟调度器也是一样,它本质上讲也是一种状态状态推演的逻辑,就是它不断的根据这个这个调度器下发的配置,这个配置是个状态目标状态,就比如说我需要其中哪些进程,这些进程它给了这些包,这些包这些这些app包,它给了这个app包的objectID,也就是说调这个调度器给了这些东西之后,它是一个目标状态。那么对于nodedemon来讲,它必须得去检测这些目标进程是否存在,那目前这个检测的这个东西里面,我不知道现在有没有把这个objectID放进去啊,因为传统的检测更加倾向于,是一种包名检测,就比如说从进程名的角度,对吧,去去检测这个进程在不在,那就是存在的。那这样的话,它其实就会导致这个自动升级无法无法实现,因为它没有办法判断现在在跑的进程,是不是调度器配置文件里的这个带哈希的这个精确版本,这就是从原来的开发模式继承下来的嘛。因为如果说你呃就相当于说我们在这里去必须得从这种过去的开发模式的这种呃这种不是那么精确的这个这个判断判断是否存在的逻辑变成一个精确的逻辑就是说跟前面一样了,如果说调度器下发了这个哈希,那这个时候他在判断的时候,判断进程是否存在的时候,他就必须得去判断它的这个完整路径。我们的这个pkg因为的这个实现它保证了这个完整路径里面一定就是说这个目录里面一定是带有这个哈希的。对吧,当然说这个这个这个基于基于这个进程的路径名去判断是否存在,还是说基于这个这个参数去判断,就比如说也可以说呃我我我我我这个不在进程里面加,但我在一个参数里面加,或者说我这个nodedemon维护维护一个列表,对吧,这个列表维护列表是 是他每次启动完了之后,他去写入一个pid和这个包名,包名加包的这个哈希的这个列表。我觉得他从这个角度来讲的话,可能去维护这个列表的角度可能会更加的准确一些,它不是那么因为这个这个查找进程并杀死这个事情,其实还是有一些平台相关性的吧,反正反正他得在这个点上,他得在这个点上,当然说当然说维护进程的缺点就是什么?缺点就是他他排除了就是他不是那种状态推导型的, 就是它假设所有的进程都是他拉了才可以,就如果说有一个别的进程,比如他一个保修员启动了一个目标目标版本的这个这个服务,对吧?那如果按照这种状态推演来讲的话,这个调度这个nodedaemon它只要看到这个进程进程,他想要的那个进程,就是他明确想要的带版本的那个进程或者在objectid那个进程只要在了,那他就啥也不干。所以这是这个自动升级的什么第一步啊,它得确认这个他就说准确的,没有任何歧义的确定他想要的那个进程是不是在运行中。

那这个自动升级它判活之后了,它就相当于它的时间的效果就是,即使是我我我,我只关心我要的目标进,就是它其实已经基于objectID还有一个好处啊,就是它其实不是那么的在意是新版本还是老版本,反正只要不是我要的这个版本在运行,我就认为不存在。那这时候来讲的话,那它认为不存在的话,那它就会去想要去启动,启动新版本。那启动新版本的时候,它就会进入一个,就是系统里的标准的这个我们我们所谓的这个lazy的一个逻辑啊,就是它是在启动这个版本之前,它还会才会去判断ENV有没有没有装这个包。就如果说没有这个装这个包的话,它就会通过这个packageID,对吧,我们的packageID是个命名对象,那我们的这个NAMSTorge是系统里面呃性能最高的分布式存储组件,就不管你在使用的哪个组哪个,这就是我们系统的一个底层核心设计,它比文件系统还要更加的轻量。就你只要得到一个对象ID,你就可以在任何一个系统的节点上去呃得到这个对象ID对应的的对象,或者是它的chunk,对吧,它会通过这个package,这个packageID它可以立刻立刻从对象存储里面把它的chunk就一个打开一个reader,那么基于这个readerunzip,对unzip到这个呃它这个packageENV的实际的这个目录里去啊,对吧,就这个能力是是我们这个系统的一个一个很底层的能力,就是说是属于可以稳定依赖的,然后它会在这个时候去做这个lazyevo去把它本地这个这个如果没装,它就会去自动装,装了的话就算了。也就是说,其实在这点来讲,它是有自修复能力的,就比如因为某些原因,这个误删除了这个目录,其实呃对我们现在的结构来讲,它是可以很,只要只要这个分布式对象存储还在,那它就可以从里面自动的还原出来。就它它它把这个谁来去安装这件事情,包括安装这个东西会不会说导致中断的这种状态异常给排除掉了。对它来讲,它就是因为这个有对象ID嘛,所以这也是我们为什么一定要对象ID的原因。作为一个分布式系统,我们如果说依赖传统的运维脚本,那就必然会碰到装一半这种问题。装一半这种问题就很尴尬了,对,所以我们这里面装这件事情,它是百分之百是原子性的,对,而且是百分之百是,呃在安装前已经对这个这个包的这个哈希做过校验的,它不会存在,永远不会存在这个下载到一半或者安装到一半的这个问题啊。 对吧,这个是它的这个就是说类似于eu的这个这个这个就本地本地本地的这个文件构建的过程,本地文件构建之后呢,它会调启动脚本,但我们现在系统里面所有启动脚本的第一行就是所谓的删除,先停止,就所有的启动脚本的第一行都是停止,为什么呢?是因为这个停止脚本里它是一个泛化的停止,就是它会停止这个包的这个系列的所有的版本。就是说换句话讲,呃真正就是这个其实有一点点反直觉啊,就是说它这个这个启动脚本里面其实它是用的是通配符,通配符去杀进程。对,但它在检测脚本里面是用的是精确的路径去去检测,就这种这种细节逻辑,我们现在因为我们现在这个这个这个这个相关的nodedemo的脚本其实实现是跟pkg env是分开的,但它其实这两边的细节它依赖挺深的,所以我在想可能可能这套脚本可能得是pkgenv可能得去作为基础设施吧,就说得升级一下,对吧,现在nodedem去靠这个pkgenv本身的一些过去的一些路径规则去拼这些脚本其实不是那么的显性,我好几次都碰到有有有工程师说,他们觉得这代码写得很奇怪,对吧,就说随手改一下,但其实就破坏了这种破坏掉了这种仔细的状态依赖,而且每上来让大家看这个代码的时候,都会都出现那种说要想一下才为什么有的地方用的是精确控制,对吧,有的地方一定要用泛的控制,而这里面任何一地方搞错其实都都对不上了。

最后是卸载啊那对于我们像现在这个system来讲的话,可以看到其实当然这还包括一个nodedaemon的自身级的一个问题,因为nodedaemon某种意义上讲它是一个维护服务嘛,就算是我们不太改就几乎不改,但它也有这个升级的可能性,对吧?如果说我们去掉nodedaemon的话,其实系统里最最正常的一个呃一个一个一个逻辑其实就是充分的利用这个这个或者说对于卸载来讲对一个一个充分的利用就是什么? 就是如果说我现在是是这个非开发版,就非开发就严格的这种版本,那么nodedaemon只需要在每一天第一次运行的时候,对吧,比如nodedaemon它会设置一个时间点,对吧,你可以认为是系统的这个清理点,它可以把这个当前pkgenv的所有的这个解压的目录全都删掉,全都删掉,那么这个时候通过lazyevil方式方式的话,需要的这个节点就会被恢复回来。就像那个时候可以通过这样的方法可以去清扫呃可以可以去清扫旧版本,而不是说呃去设计一个非常复杂的逻辑,对对吧,因为按照刚刚的设计,这个旧版本就没法清理了嘛。对吧,它得去做这个事情,让它自己的清理的话,可能是这个nodedaemon自己它要挂一个非常非常小的脚本。就这个非常非常小的脚本,它只做一件事情,就是呃把nodedaemon先删掉,删掉之后呢,再等这个lazyevil对吧,当然它肯定得得明确的知道这个这个旧版本,这个新版本是存在的啊,所以它得基于这样的一个逻辑,就在nodedaemon每次启动前,就nodedaemon不是说它是一个直接启动的进程,它是通过一个一个永远不会改变的这个nodedaemon的start脚本去启动的对吧,然后这样的话就可以做到呃系统里面所有的对象本身哈它它不会说出现说它长期运行下去之后pkgenv这个出现这种所谓的这个版压缩版本爆炸的爆炸的问题,这个在过去的这个这个这个事情这个样子其实跟过去我们所面对的这个python的依赖爆炸和这个这个npm的依赖爆炸,其实是是一样的。所以说我们呃后面引入了这个objectid之后和lazyevil的时候,其实通过一个比较简单的方法就可以解决这个问题。


过去里面去用这个因为还有一个跟我们之前使用方式不同的是,在今天这个结构下,其实这个自己带的那个databse变得没有那么的重要了,因为原来的那个database更多是支持版本运算用的。就是当时这对它肯定还是要保持这个功能的嘛,毕竟还有别的系统在用对吧,但我们今天其实已经没有什么本地的版本系统要做了,其实很多时候这个这个database这个其实就变成了说,呃我从调度器这边拉回来一个一个一个包对吧,那个包那个包我可以我可以立刻马上的,就把它拉回来的那个objid对应的那个meta文件塞到这个数据库里面去对吧,就换句话讲,换句话说,包括我们之前呃有些环境下做自动升级的逻辑是怎么怎么做的呢?是是是当时当时说那个时候没有调度没有这个nodedaemon这套东西啊。比如那个时候用来做传统的软件系统,怎么做自动升级的呢?他就是把这个packageenv里面那个database那个那个meta数据库定期的跟服务器的我们叫或者叫源的这个database做一下同步对吧,就下载回一大堆来,那这个时候我们也同步完这个database之后,那个lst的版本就改变了嘛。那个版本改变了之后呢,然后完了之后这个packageenv再去做这个lazyw对吧,lazyw这个特性当时我们一直都有,对吧,就做lazyw,然后他就把新版本给加回来了。对吧,这是过去的逻辑啊。过去的逻辑是依赖说呃一个这个packageenv的database的一个这样的一个升级和分发的一个过程呢。但但这个这个过程其实现在至少在我们的这个每个节点的这个运行环境上来讲,它可以没有那么的重型,因为对他来讲,只需要这个packageenv这个它本身在这个packageenv里是个缓存的嘛。所以它这个packageenv的database里面,它只要有调度器nodedaemon下发的那些包就可以,那些package的这个meta就好了。而偏偏这个meta刚好调度器再去创建这个机床运行的进程的时候,他又把这个meta全都存起来了。也就是说这个里面其实这个这个旧的这个database的语义,其实在我们现在的这个环境下其实不不应该去用它。但如果说都不用它的话,甚至我们可以退化成纯开发语义,就这样的话,大家也不会去,就开发模式语啊,大家也不会去纠结纠结这个,就是换句话讲,换句话也不会去纠结说这个database的数据对不对这种问题对吧,特别是这个数据库本身,我们刚刚讲三缓存是很容易的,但三数据库,我们好像之前从来没算过这个这个db的数据库,对吧,因为它里面量比较小嘛。对吧,就轻缓存的过去都不行这玩意啊。那如果说今天我们所有的这个这个这个数据库都是每次调度器下发的配置里面这个零碎收集起来的话,那我觉得它就相当于可以整个目录就基本上可以回到一个清空的状态了。

最后就是带来了一个纠结的点嘛, 就通过整理上面的这个东西可以发现,其实过去作为一个核心组件的这个这个packageenv啊,就应该说我们这个系统在早期的时候,因为任何系统都是要做包管理和自动升级的嘛,这个组件我们也做了,就用了很长时间了,对吧,想都没想就把这个packageenv给引进来了,当然我们还做了python到rust的移植了。那现在来看呢,其实我们根本不需要这么强力的组件,对吧,可能现在对我们来讲,也许一个更加轻量级的一个nodedaemon管理的一个简单的目录规则的组件也许就能解决这个问题。对吧,就说这里面一些特性其实相对来讲都是比较简单的,对吧,无非就是有一个有一个install的函数,就是lazyEvil,最重要的就是这个lazyEvil的这个这个install函数。如果有一组目录规则,对吧,既然没有数据库了之后,也没有这个版本的这个这个有缓存之后,其实这些事情都变得比较纯粹啊。所以说这是个比较纠结的问题,这又是性能很很早就写的东西,对吧,删除它的话,或者把它把它变成一个简单的东西,会不会带来带来这个一些一些风险啊,但我们现在需要的东西跟这个原来老的这个packageenv有一些区别,对吧,那去用去修改老的packageenv,让它让上游去加这些东西去让我们跑得更好,对吧,会不会也让packageenv变得没有别人那么纯粹?

最后再站在这个包的更新,就是站在发行者的角度,我们看一下这个全部的流程啊。首先第一个,这个任何一个包它的这个发行者,他会首先他需要把他的这个包构建好之后呢,生成相关的这个objectid,然后上传到自己的这个warpserver上,就是说然后第二个他要把这个更新给广播出去,也就是说他得把它的这个这个最新版本的这个packageid,packagemeta这个packagemeta里面就包含了这个最新的版本嘛,把这个packagemeta广播出去,然后呢,根据我们现在这个整个系统里面所有的人去跟踪一个package的这个新版本packagemeta的逻辑是两个地方,就应该说有三个地方吧,就第一个最权威的是Bns,当然这个更新成本比较贵啊,就是说原理上讲,其实一般大家不会说选择这里,但这里是最稳的,就是区块链上面嘛,就是说发行商他有自己的一个bns然后把它更新上去。然后第二个就是索引器,就或者说包索引器吧, 就很我们允许这个这个应用使用者订阅多个源,对吧?那这个源的本质其实就是它有一大堆的这个packagemeta,对吧,反正这些都是可以本地做验证的,因为这个packagemeta都有签名嘛,对吧,所以说它在下载的时候一定会做验证的。 就说这是第二个,就是他把它把的这个带签名的packagemeta塞到源上面去,各种源上面去。然后第三个就他自己了。就比如说我现在已经知道这个这个makepackage就说我已经下了之后,我其实是知道你的这个owner的名字的,那我取这个owner的名字,我可以反推出一个这个owner的这个ood的这个域名,那我通过这个域名的话,我可以用这个标准的这个wrap协议去你这里去查,对吧,去查这个objectid是否存在,对吧,这个这个或者说我通过package名去查,说通过一个名字去查他这个list的出来,他所有这个repo上面。 这个作为作者他所发行的版本,这个是他的首先第一步,就是说他需要去把新版的packageMeta构造出来,并且存在至少存在三个位置里面的至少一个吧,至少一个。 然后第二个,这边OOD这边,就是说这个待升级这边,他通过某些原因吧,反正不管是自动的还是手动的,对吧,反正他查到这个meta之后呢,他会把这个根据他的这个业务逻辑啊,他会把这个meta这个用我们的术语叫做先collect,对吧,先收藏,收藏到自己的自己的repo上, 就把这个packageMeta这个小的元数据先给收藏下来,然后呢,再触发这个就是说让用户知道,OK,这个发现了一个新版本了,对吧,然后根据逻辑,当时有的是通知用户自动下载,有的可能就有的系统看系统配置了,对吧,那有的可能就自动下载了。 对吧,然后他会把这个packageMeta对应对应的这个文件给下载到repo的这个name的store上去,就相当于这个逻辑是跟任何一个packageE都没有关系的,它本质上讲就是一个经典的流程,我有一个内容,这个内容有一个名字,然后这个名字有了一个新的版本,然后这个新版本的id是多少,对吧,然后我拉了这个id之后呢,我把它收藏到repo上去,根本上这是一个业务无关的逻辑啊,就是说跟那个包加载没关的逻辑,就是我们的内容的基本,就是所有内容都有的这种逻辑,就是名字和id的一个升级的关系,就是说我同一个名字,它对对象id升级了的一个逻辑。 然后呢,这个升级好了之后呢,然后下面就是这个用户手工触发升级了,那手工升级的流程的核心就是在调度器里面把这个应用的配置文件的objectID从旧版本改到了新版本,对吧,当然按照我们说法去调度器也不是很知道是旧还是新,反正总之就是通过业务逻辑把这个包的id就是说更新到了一个指定版本吧。 然后说如果说我这本来就是开发版的,对吧,开发版的,那这个时候呃我我装了一个这个这个生产环境的一个包的话,其实也OK,对吧,就相当于说你开发版的时候,你那些那些已装的应用都是你在命令行里面或者在开发工具里面敲进去的嘛,没有objectID嘛,然后在这个正式你通过正式的原装的话都有objectID嘛。然后这个有objectID之后呢,然后再触发到nodeimore对吧,然后nodeimore里面它这边它去真正启动进程的时候,它可能在另一台机器哈,那他这个时候它走的就是一个这个全全重列分布式系统都可见的这个在这个lazyevil的一个逻辑,对吧,他拿到这个东西之后,他就他就通过配置它它就确保在这个那么的search里拿, 就像那个我们的核心容错,就是说系统的可靠性保证其实是在调度期构造配置文件之前,就就相当于说对于底层设施来讲的话,他们走到他们的流程的时候,他们都假设这个东西在这个中内已经存在了,说他们不会说再去折腾任何跟中外打交道的事情了,他们只需要知道说你想你把这个包要我要我去运行指定的版本, 然后呢,这个指定的版本所要的这些这些文件你必须得在那个store里面先存好,然后我后面这个lazyevil我能成就行了。至于你从哪里弄来的,对吧,它其实不是那么的就不不关心这个细节。
```