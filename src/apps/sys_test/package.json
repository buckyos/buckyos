{
  "name": "buckyos_systest",
  "private": true,
  "type": "module",
  "dependencies": {
    "buckyos": "latest"
  },
  "scripts": {
    "build:web": "pnpm --filter ./web run build",
    "build": "pnpm run build:web && node prepare.mjs"
  }
}
