{
  "name": "buckyos_systest",
  "private": true,
  "type": "module",
  "dependencies": {
    "buckyos": "github:buckyos/buckyos-websdk#main"
  },
  "scripts": {
    "build:web": "pnpm install && pnpm update buckyos && pnpm --filter ./web run build",
    "build": "pnpm run build:web && node build.mjs"
  }
}
