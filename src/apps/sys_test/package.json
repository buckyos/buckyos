{
  "name": "buckyos_systest",
  "private": true,
  "type": "module",
  "dependencies": {
    "buckyos": "git+https://github.com/streetycat/buckyos-websdk#refactor/aicc"
  },
  "scripts": {
    "build:web": "pnpm --filter ./web run build",
    "build": "pnpm run build:web && node prepare.mjs"
  }
}
